"use strict"; 

// Déclaration des constantes (Slide 37)

const NOM = "John";     // Chaîne de caractère (Type primitif String - Slide 37)
const PRENOM = "Doe";   // Chaîne de caractère (Type primitif String - Slide 37)
const AGE = 30;         // Nombre entier (Type primitif Number - Slide 37)
const NOTE = 15;        // Nombre entier (Type primitif Number - Slide 37)
const PI = 3.14;        // Nombre décimal (Type primitif Number - Slide 37)


// Définition de la fonction f1() : Afficher le Nom (Slide 45, 134)
function f1() {
    
    console.log(NOM);
}

// Définition de la fonction f2() : Afficher le Prénom (Slide 45, 134)
function f2() {
    console.log(PRENOM);
}

// Définition de la fonction f3() : Afficher le Nom & Prénom (Slide 45, 134)
function f3() {

    console.log(`${NOM}-${PRENOM}`);
}

// Définition de la fonction f4() : Afficher l'Age (Slide 45, 134)
function f4() {
    console.log(AGE);
}

// Définition de la fonction f5() : Afficher la Note (Slide 45, 134)
function f5() {
    console.log(NOTE);
}

// Définition de la fonction f6() : Afficher Pi (Slide 45, 134)
function f6() {
    console.log(PI);
}